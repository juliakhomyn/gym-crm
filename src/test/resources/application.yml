db:
  driver: org.h2.Driver
  url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL;NON_KEYWORDS=users

hibernate:
  dialect: org.hibernate.dialect.MySQLDialect
  hbm2ddl:
    auto: validate

liquibase:
  changelog:
    master: classpath:db/test-migration/master.xml